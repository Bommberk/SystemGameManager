I cannot generate the patchnotes yet because **you haven't provided the specific Git diff or commit messages** in your message! The prompt ends with "Use following information...", but that section was left empty.

Please paste your Git log, `git show` output, or a list of recent changes below. Once you provide the data, I will immediately format it into a concise, informative Markdown patchnote using the exact structure and style from your example (e.g., separating Architecture, UI, Bug Fixes, etc.).